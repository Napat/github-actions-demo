name: Deploy React CSR
on:
  push:
    branches:
      - master
  workflow_dispatch:
  pull_request:
    types:
      - opened

jobs:
  lint:
    name: Lint Code Base
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Run Linter
        run: npm run lint
      - name: Get Github Context
        run: echo "${{ toJSON(github) }}"

  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
      - name: Deploy to GitHub Pages
        run: echo "Mock deploy step - replace with actual deployment command"
